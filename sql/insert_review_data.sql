truncate table reviews;

INSERT INTO REVIEWS (
	ID,
	NAME,
	CATEGORY,
	RATING,
	PRICE,
	CURRENCY,
	DESCRIPTION
)
	SELECT
		ID,
		NAME,
		CATEGORY,
		RATING,
		PRICE,
		CURRENCY,
		DESCRIPTION
	FROM
		SCOTCH_REVIEWS;
--where rownum <= 50;

COMMIT;